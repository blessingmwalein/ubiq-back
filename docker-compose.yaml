version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    container_name: laravel_app
    restart: always
    ports:
      - "9000"
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
